# CSV to Excel Converter

A simple Python tool that converts CSV files into Excel (.xlsx) format with basic data cleaning and normalization.

## Features

* Reads CSV files using pandas
* Handles missing values
* Cleans and formats column names
* Converts data to Excel format
